# Python
__pycache__/
*.pyc

# Model checkpoints (large binaries, regenerable from the notebooks)
checkpoints/
checkpoints_masked/
checkpoints_segmentation/

# Datasets (large, regenerable/re-downloadable, not source code)
dataset/
tomato final dataset/
tomato final dataset_masked/

# Exported model packages (large binaries, regenerable from the notebooks)
export/
export_leaf_pipeline/
export_leaf_pipeline_unet/
